spring:
  application:
    name: e-commerce

  datasource:
    url: jdbc:mysql://localhost:3307/db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          batch_size: 100
        default_batch_fetch_size: 500
        show_sql: true
        format_sql: true

token:
  secret: and0LXRva2VuLXNlY3JldA==sdfdsfadasfdasfdasfdasfdasfasd # 임의의 문자열
  access-token-expiration-ms: 900000 # 15분 1000(ms) x 60(s) x 15(m)
  refresh-expiration-ms: 1209600000 # 2주 1000(ms) x 60 (s) x 60(m) x 24(h) x 14(d)